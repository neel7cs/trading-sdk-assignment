# Trading Platform SDK - Assignment

A simulated Trading Platform backend (Node.js/Express) and a Client SDK wrapper designed to demonstrate core trading functionalities.

## 📂 Project Structure

```text
bajaj-trading-project/
├── sdk/                # Wrapper Library (The "Client" logic)
├── server/             # Backend API (The "Platform" logic)
│   ├── data/           # In-memory data storage
│   └── routes/         # API endpoints (Instruments, Orders, Portfolio)
├── tests/              # Demo script to verify functionality
└── README.md
